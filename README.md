# dill.pkl

Project layout:

- `front end/` contains the React + Vite frontend
- the repo root is available for the Python backend

To run the frontend:

```bash
cd "front end"
npm install
npm run dev
```
